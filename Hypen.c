//Write a program to replace spaces in a string with hyphens.
#include<stdio.h>
#include<string.h>
main(){
	char a[20],i;
	printf("Enter a string");
	gets(a);
	a[i]=='-';
	for(i=0;a[i]!='\0';i++)
	{
		printf("%c",a[i]);
	}
}
